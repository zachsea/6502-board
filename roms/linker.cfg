MEMORY {
    ROM:     start = $8000, size = $7FFA, file = %O, fill = yes, fillval = $00;
    VECTORS: start = $FFFA, size = $6,    file = %O;
}

SEGMENTS {
    CODE:    load = ROM,     type = ro;
    VECTORS: load = VECTORS, type = ro;
}
